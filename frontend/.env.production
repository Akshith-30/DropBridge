# Baked into Vite production builds (Vercel). Override in Vercel dashboard if URLs change.
VITE_API_BASE_URL=https://dropbridge-api.onrender.com
VITE_WS_URL=wss://dropbridge-api.onrender.com

# TURN — add in Vercel when you have Metered credentials (cross-NAT P2P)
# VITE_TURN_URL=turn:global.relay.metered.ca:80
# VITE_TURN_USERNAME=
# VITE_TURN_CREDENTIAL=
