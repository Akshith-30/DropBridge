# Loaded automatically by Vite for `npm run dev` (local development).
# Overrides frontend/.env — do not put production URLs here.

VITE_USE_LOCAL_BACKEND=true
VITE_API_BASE_URL=
VITE_WS_URL=

# Optional: cross-NAT P2P testing on localhost
# VITE_TURN_URL=turn:global.relay.metered.ca:80
# VITE_TURN_USERNAME=
# VITE_TURN_CREDENTIAL=
