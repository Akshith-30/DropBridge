# Used by Vite in dev and production builds (public URLs — safe to commit).
VITE_API_BASE_URL=https://dropbridge-api.onrender.com
VITE_WS_URL=wss://dropbridge-api.onrender.com

# Set to true only when running backend locally (mvn spring-boot:run) + Vite proxy
# VITE_USE_LOCAL_BACKEND=true
