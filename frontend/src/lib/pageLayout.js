/** Shared layout classes for routed pages (below fixed navbar). */
export const PAGE_MAIN =
  'flex min-h-[calc(100vh-4.5rem)] flex-1 flex-col items-center px-4 pb-10 pt-[5.5rem]';

export const PAGE_CONTAINER = 'flex w-full max-w-[40rem] flex-col gap-5';

export const PAGE_CONTAINER_NARROW = 'flex w-full max-w-md flex-col gap-5';
