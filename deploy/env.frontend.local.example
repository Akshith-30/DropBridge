# Optional: frontend/.env.local (gitignored) — overrides .env.development
# Copy: cp deploy/env.frontend.local.example frontend/.env.local

# Hit production API from local UI (debugging only):
# VITE_USE_LOCAL_BACKEND=false
# VITE_API_BASE_URL=https://dropbridge-api.onrender.com
# VITE_WS_URL=wss://dropbridge-api.onrender.com
